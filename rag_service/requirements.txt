# FastAPI và server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
httpx>=0.25.0  # HTTP client để gọi backend API

# Document processing
python-docx>=1.1.0
PyPDF2>=3.0.0
openpyxl>=3.1.0

# Embeddings
sentence-transformers>=2.2.0
openai>=1.3.0  # Optional: nếu muốn dùng OpenAI embeddings

# Image processing
Pillow>=10.0.0  # Image processing
torch>=2.0.0  # PyTorch for CLIP model
torchvision>=0.15.0  # TorchVision for image transforms
# CLIP model - install from GitHub
# Note: CLIP must be installed separately: pip install git+https://github.com/openai/CLIP.git
# Or add to requirements: git+https://github.com/openai/CLIP.git

# Vector stores
chromadb>=0.4.0  # Chroma vector database
# pymilvus>=2.3.0  # Uncomment nếu muốn dùng Milvus thay vì Chroma

# Utilities
numpy>=1.24.0,<2.0.0
pydantic>=2.5.0
python-dotenv>=1.0.0  # Load environment variables từ .env file

# Logging
python-json-logger>=2.0.0

# Database export (optional - chỉ cần khi chạy export_database_to_markdown.py)
pyodbc>=5.0.0

