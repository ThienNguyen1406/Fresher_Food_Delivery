# Minimal requirements - Cài đặt từng bước để tránh lỗi SSL/timeout
# Chạy: pip install -r requirements-minimal.txt

# Bước 1: Core dependencies (nhỏ, cài nhanh)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# Bước 2: Document processing (nhỏ)
python-docx>=1.1.0
PyPDF2>=3.0.0
openpyxl>=3.1.0

# Bước 3: Utilities
numpy>=1.24.0,<2.0.0
python-json-logger>=2.0.0

# Bước 4: OpenAI (optional, nhỏ)
openai>=1.3.0

# Bước 5: ChromaDB (cài riêng nếu cần)
# chromadb>=0.4.0

# Bước 6: Sentence Transformers (lớn, cài cuối cùng)
# sentence-transformers>=2.2.0

